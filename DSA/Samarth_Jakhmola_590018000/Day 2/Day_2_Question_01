def sum_even(nums):
    sum = 0
    for i in range(len(nums)):
        if nums[i] % 2 == 0:
            sum += nums[i]
    return sum

nums = list(map(int, input("Enter the array elements separated by spaces : ").split()))
print(sum_even(nums))
